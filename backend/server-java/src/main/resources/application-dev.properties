# Development profile — use MySQL for local dev (configure via env or use defaults below)
server.port=5000

# Use MySQL for development to match production environment. Override with env vars if needed.
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:educadb}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:Hari@7893504891}

# Initialize schema on startup in dev
spring.sql.init.mode=always
spring.datasource.initialization-mode=always

# Use JDBC session store
spring.session.store-type=jdbc

app.cors.allowedOrigin=http://localhost:3000
